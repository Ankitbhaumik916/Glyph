# --- Signature verification backend dependencies ---
# Install into a venv (see README.md). CPU-only torch is plenty for inference:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9          # required by FastAPI for multipart/form-data uploads

torch>=2.2,<3.0

# NOTE: opencv-python <4.10 is compiled against NumPy 1.x and fails to import
# under NumPy 2 with "_ARRAY_API not found". Keep these two in step.
numpy>=2.0,<3.0
opencv-python>=4.10,<5.0

scikit-image>=0.24,<1.0
# Imported directly by app/forensic_features.py (wasserstein_distance). It
# arrives transitively with scikit-image today, but an import we make ourselves
# gets declared ourselves - otherwise a future scikit-image release that drops
# or repins it breaks the Space build with no warning.
scipy>=1.11,<2.0
pillow>=10.3,<12.0

# Writes reviews to a private dataset repo (see app/reviews.py).
huggingface-hub>=0.25,<1.0
